<odoo>
    <record id="invoice_action" model="ir.actions.act_window">
        <field name="name">Invoice</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">list, form</field>
   </record>

    <record id="list_invoice" model="ir.ui.view">
        <field name="name">list.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <list>
                <field name="student_id"/>
            </list>
        </field>
    </record>

    <record id="form_invoice" model="ir.ui.view">
        <field name="name">form.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <group>
                <group col="1">
                    <field name="student_id" readonly="state == ('posted')"/>
                </group>
            </group>
        </field>
    </record>
</odoo>